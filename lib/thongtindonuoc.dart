import 'package:flutter/material.dart';

class ThongTinDoNuocScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thông tin đo nước'),
      ),
      body: Center(
        child: Text('Nội dung của trang "Thông tin đo nước"'),
      ),
    );
  }
}
